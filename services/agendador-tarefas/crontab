# a cada 5 minutos
*/5 * * * * curl http://sistemas.${DOMINIO}/?cron-5min

# a cada hora
0 * * * * curl http://sistemas.${DOMINIO}/?cron-horario

# sempre as 2h da madrugada
0 2 * * * curl http://sistemas.${DOMINIO}/?cron-diario
