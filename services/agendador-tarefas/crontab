# a cada 5 minutos
*/5 * * * * curl https://sistemas.${DOMINIO}/?cron-5min

# a cada hora
0 * * * * curl https://sistemas.${DOMINIO}/?cron-horario

# sempre as 2h da madrugada
0 2 * * * curl https://sistemas.${DOMINIO}/?cron-diario
